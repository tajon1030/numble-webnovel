spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/numble-webnovel?serverTimezone=Asia/Seoul
    username: root

  sql:
    init:
      mode: always